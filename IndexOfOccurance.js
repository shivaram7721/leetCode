var strStr = function(haystack, needle) {
    let result = haystack.indexOf(needle);
    return result;
};

// let haystack = "sadbutsad", needle = "sad"
let haystack = "leetcode", needle = "leeto"
let res = console.log(strStr(haystack, needle))